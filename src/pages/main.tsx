import React from 'react';
import Dashboard from "@/components/dashboard";

const Main = () => {
    return (
        <main className={'main'}>
            <Dashboard/>
        </main>
    );
};

export default Main;